source("renv/activate.R")

# Define repos to install packages from
options(repos = c(binaries = "https://packagemanager.rstudio.com/all/__linux__/centos7/latest",
                  source = "https://packagemanager.rstudio.com/cran/latest"))
